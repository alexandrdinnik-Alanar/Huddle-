generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// S0-006 establishes Prisma and PostgreSQL only.
// Identity and product models begin in later approved tasks.
