# NEXT TASK

S0-001, S0-002, S0-003, S0-004, S0-005, S0-006 and S0-007 are merged.

Current task:

```text
S0-008 - Seed initial Huddle roles
```

Status:

```text
READY - S0-007 merged; begin initial role seed work. S0-008 depends on S0-007 and must seed only the canonical initial Huddle roles without creating users, credentials, fake feature data, or starting S0-009 auth work.
```

Task contract:

```text
tasks/phase-0/S0-008.md
```

Canonical visual identity source:

```text
Brand_Guidelines.zip
```

The uploaded package is authoritative for Huddle colors, logotype, icon/community mark, typography direction and broader visual identity. Older set50-derived visual values are reference-only when conflicting.

Contract title: `Seed initial Huddle roles`

Roadmap tracking label: `Seed roles`

S0-008 must implement only the initial Role-table seed on top of the merged S0-007 identity base. Do not create users, admin credentials, fake marketplace or event data, UserRole assignments, permission/auth implementation, or any S0-009 work in this task.
