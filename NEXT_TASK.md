# NEXT TASK

S0-001, S0-002, S0-003, S0-004, S0-005 and S0-006 are merged.

Current task:

```text
S0-007 - Implement User / Role / UserRole models
```

Status:

```text
READY - S0-006 merged; begin identity-base schema work. S0-007 depends on S0-006 and must not implement auth, permissions, or feature-domain models.
```

Task contract:

```text
tasks/phase-0/S0-007.md
```

Canonical visual identity source:

```text
Brand_Guidelines.zip
```

The uploaded package is authoritative for Huddle colors, logotype, icon/community mark, typography direction and broader visual identity. Older set50-derived visual values are reference-only when conflicting.

S0-007 must implement only the identity-base Prisma schema for User, Role, and UserRole. Do not implement auth, permissions, sessions, marketplace models, event models, family models, admin feature models, moderation/report models, or any product feature code in this task.
