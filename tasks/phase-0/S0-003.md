# S0-003 — Add approved brand assets and logo components

## Status

`IN PROGRESS — SOURCE VERIFIED; EXACT BINARY REPO IMPORT PENDING`

Tracking issue: `#6`

## Metadata

| Field | Value |
|---|---|
| Task ID | `S0-003` |
| Phase | Phase 0 |
| Priority | P0 |
| Dependencies | `S0-002` merged |
| Current state | IN PROGRESS |
| Source package | `Brand_Guidelines.zip` verified |
| Agent | Claude Code or Codex |

## Objective

Import the exact approved Huddle brand binaries and implement safe React logo components that reference only verified supplied assets.

## Source precedence

The uploaded master brand package declares this hierarchy:

1. identity masters;
2. approved visual reference PNG;
3. editable layered guideline source;
4. exact design tokens;
5. reusable vectors.

Its identity policy explicitly forbids redraw, AI recreation and distortion. Accepted repository ADRs remain binding until conflicts are reconciled through review.

## Source audit result

The uploaded `Brand_Guidelines.zip` was inspected directly.

- required S0-003 assets found: `12 / 12`;
- source SHA-256 calculated: `12 / 12`;
- transparent variants inspected for alpha: PASS;
- both large split archive SHA-256 values match the included 2-part manifest: PASS;
- no cropping, tracing, AI generation or CSS recoloring is required.

The exact verified checksums are recorded in:

```text
public/brand/expected-assets.json
```

Current remaining gate: exact binary files must be physically present at repository paths and re-verified there.

## Required approved wordmark assets

```text
public/brand/logotype/Huddle_logotype_copper_transparent_APPROVED.png
public/brand/logotype/Huddle_logotype_dark_green_transparent.png
public/brand/logotype/Huddle_logotype_white_transparent.png
public/brand/logotype/Huddle_logotype_copper_on_deep_green.png
public/brand/logotype/Huddle_logotype_dark_green_on_ivory.png
public/brand/logotype/Huddle_logotype_white_on_deep_green.png
```

## Required approved icon assets

```text
public/brand/icon/Huddle_icon_copper_transparent_SELECTED.png
public/brand/icon/Huddle_icon_black_transparent.png
public/brand/icon/Huddle_icon_white_transparent.png
public/brand/icon/Huddle_icon_copper_on_deep_green.png
public/brand/icon/Huddle_icon_dark_green_on_ivory.png
public/brand/icon/Huddle_icon_white_on_deep_green.png
```

## Component implementation

Implemented on the S0-003 branch:

```text
src/components/huddle/brand-assets.ts
src/components/huddle/BrandLogo.tsx
src/components/huddle/IconWatermark.tsx
```

`BrandLogo` must:

- render verified supplied logo assets only;
- support only typed manifest-backed variants;
- support meaningful alt text;
- support dimensions and/or `className`;
- never render a typed text logo;
- never recolor assets with CSS filters.

`IconWatermark` must:

- render verified supplied icon assets only;
- support only typed manifest-backed variants;
- default to decorative `aria-hidden` behavior;
- allow watermark/background/seal use;
- not invent a new lockup.

## Explicitly forbidden

- crop from `510_logo_usage_brand_rules.png`;
- redraw or trace geometry;
- AI-generate a replacement;
- type `Huddle` as a logo substitute;
- create SVG recreations from screenshots;
- recolor with CSS `filter`;
- invent missing variants;
- silently use another brand's asset.

## Required checks

- exact repository binaries match recorded SHA-256;
- `pnpm lint`;
- `pnpm typecheck`;
- `pnpm test`;
- `pnpm build`.

## Acceptance criteria

- [ ] exact approved binary assets are physically present in repository paths;
- [x] source package contains all 12 required assets;
- [x] source SHA-256 manifest is complete;
- [x] `BrandLogo` exists on task branch;
- [x] `IconWatermark` exists on task branch;
- [x] components reference approved typed paths only;
- [x] no generated/redrawn/cropped logo asset exists;
- [x] no CSS recoloring exists;
- [x] unit tests cover approved variant maps and directory restrictions;
- [ ] repository-binary checksum verification passes;
- [ ] required CI checks pass on final head;
- [ ] `docs/execution/S0-003/HANDOFF.md` exists.

## Stop rule

Do not start S0-004 until exact binaries are present, repository checksums match, CI passes and S0-003 is merged.

Before S0-004, separately reconcile the uploaded master guideline token/typography values with the current set50-derived ADR baseline.
