# 15 QA, Manifests and Archive

This section contains package inventory, hashes, duplicate reports, transfer status and supplemental assets.

AI and human operators should use manifests to verify file identity and avoid accidental duplication.

Large binary files that cannot be transferred through the connected GitHub tool must be listed explicitly in `NOT_UPLOADED_BINARY_FILES.md` rather than silently omitted.