# Claude Design Canonicalization Index

The raw Claude Design corpus was merged through PR #29 as reference material.

The follow-up canonicalization changes are intentionally separated from the raw corpus import.

Required records:

- `claude-design-canonicalization-report.md`;
- `claude-prototype-inventory.csv`;
- `claude-route-canonicalization.csv`;
- `../05-execution/claude-design-codex-task-pack.md`.

No Claude prototype is approved for React implementation unless the corresponding route has exact approved Figma node IDs in `screen-route-registry.csv`.
