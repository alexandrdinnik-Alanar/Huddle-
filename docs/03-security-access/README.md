# Security and Access

Authorization uses RoleKey plus Capability plus ScopedGrant plus object policy. Client visibility is not authorization. Every privileged read and mutation requires server-side enforcement.
